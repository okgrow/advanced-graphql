import styled from 'styled-components';

const Title = styled.div`
  font-size: 6rem;
  font-weight: bold;
  letter-spacing: 0.1rem;
  text-align: center;
  color: var(--paleBlue);
  flex: 1;
  max-height: 15rem;
  display: flex;
  align-items: center;
`;

export default Title;
